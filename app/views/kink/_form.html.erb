<%# locals: (kink:) -%>

<%= form_with model: kink, url: kink_path(kink.id), id: :kink_form, class: 'kink_form' do |f| %>
  <%= f.text_field :name, placeholder: 'Enter a kink' %>
  <%= f.submit '+' %>
<% end %>